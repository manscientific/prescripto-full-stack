fastapi
uvicorn
pymongo
python-dotenv
opencv-python-headless
numpy

# Prebuilt dlib (prevents Render from compiling it)
dlib==19.24.2

# Face recognition library (uses preinstalled dlib)
face-recognition==1.3.0
